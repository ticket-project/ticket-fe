'use client';

/**
 * 에러 페이지
 */

const Error = () => {
  return (
    <div>
      <h2>문제가 발생했습니다!</h2>
      {/* <button onClick={() => reset()}>다시 시도</button> */}
    </div>
  );
};

export default Error;
