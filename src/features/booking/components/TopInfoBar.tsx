import { Box, Container } from '@mui/material';

const TopInfoBar = () => {
  return (
    <Container
      maxWidth={false}
      sx={{ borderBottom: '1px solid green', background: 'pink' }}
    >
      TopInfoBar
    </Container>
  );
};

export default TopInfoBar;
